[package]
name = "runlog-recorder"
version = "0.1.0"
edition = "2021"

[dependencies]
windows-capture = "1.5.0"
windows = { version = "0.61", features = [
    "Win32_Graphics_Direct3D11",
    "Win32_Graphics_Dxgi_Common",
] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
image = { version = "0.25.10", default-features = false, features = ["jpeg"] }

[profile.release]
opt-level = 3
lto = true

[patch.crates-io]
# windows-capture 1.5 describes the encoder input stream with the output
# dimensions. Sending a larger WGC surface then crops instead of scaling.
windows-capture = { path = "vendor/windows-capture" }
